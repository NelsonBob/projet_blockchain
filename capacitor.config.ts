import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'projet_blockchain',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
